let myName = "Jocelyn";
let myCity = "Chicago";
console.log (`My name is ${myName}. My favorite city is ${myCity}.`)

//A template literal is used to log strings together. It is wrapped with `` and inside use a placeholder that looks like this: ${x}.
