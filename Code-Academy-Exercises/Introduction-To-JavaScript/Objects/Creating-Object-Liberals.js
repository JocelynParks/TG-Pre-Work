// Write your fasterShip object literal below
let fasterShip = {
  'Fuel Type':'Turbo Fuel',
  color: 'silver'
}
//Use {} to designate an object literal. Key-value pairs work together as a key (variable) points to a location in memory (value). Use colon to set descriptor (in this case, 'turbo fuel' and 'silver'). Seperate those two using a comma.
