let newYearsResolutions = ['Keep a journal', 'Take a falconry class', 'Learn to juggle'];

console.log(newYearsResolutions);
//An array is a list that programmers use to store data (each item has a numbered position). The listed items are separated with a comma.
