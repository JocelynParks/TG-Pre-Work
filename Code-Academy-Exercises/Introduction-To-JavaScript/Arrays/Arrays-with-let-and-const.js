let condiments = ['Ketchup', 'Mustard', 'Soy Sauce', 'Sriracha'];

const utensils = ['Fork', 'Knife', 'Chopsticks', 'Spork'];
condiments[0] = ('Mayo');
console.log(condiments);

condiments = Mayo = ['1'];

utensils[3] = ('Spoon');
console.log(utensils);

//Remember to declare variables, you may use let and const (used more even though you may not reassign values with this keyword). The above example uses "let" therefore we can reassign an element.
