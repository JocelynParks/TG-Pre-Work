const city = 'New York City';

const logCitySkyline = () =>
{
  let skyscraper = 'Empire State Building';
  return 'The stars over the ' + skyscraper + ' in ' + city;
};

console.log(logCitySkyline());
//Blocks are the contents found inside of {curly brackets}. You also group one or more statements using blocks. It's important to understans blocks before moving onto to scope.
