const logVisibleLightWaves = () => {
  let lightWaves = 'Moonlight';
	let region = 'The Arctic';

  if (region === 'The Arctic'){
    let lightWaves = 'Northern Lights';
    console.log(lightWaves);
  }
  console.log(lightWaves);
};
logVisibleLightWaves();
//Black scope allows programmers to define variables with precision without polluting the global namespace. This concept focuses on organizing code so it is easier to maintain your code.
