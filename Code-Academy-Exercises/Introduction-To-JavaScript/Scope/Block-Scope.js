const logVisibleLightWaves = () => {
  const lightWaves = 'Moonlight';
  console.log(lightWaves);
};

logVisibleLightWaves();


//Block scope are known as local varables and variables are defined inside of the curly braces. Recieved an error after the code was ran.
