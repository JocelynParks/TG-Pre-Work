
//The 'install' command creates a folder and copies package files onto it. It also installs all of the dependencies for the given packages. Once you npm install packages, you can find the Babel packages and all their dependencies in the respective folder.
//Once again, this is ran in the terminal. Enter ls, Use npm with the -D flag to install the Babel command line package , Use npm with the -D flag to install the Babel preset environment package.
