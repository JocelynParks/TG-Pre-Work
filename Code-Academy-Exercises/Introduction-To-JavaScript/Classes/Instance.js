class Surgeon {
  constructor(name, department) {
    this.name = name;
    this.department = department;
  }
}

const surgeonCurry = new Surgeon('Curry', 'Cardiovascular');
const surgeonDurant = new Surgeon('Durant', 'Orthopedics');

//An instance is an object that contains the property names and methods of a class. In this exercise, I created an instance of the Surgeon class and set the name and the department. Then, created an instance of the surgeon class.
