const plantNeedsWater = (day) =>
{
  if (day === 'Wednesday') {
    return true;
  } else {
    return false;
  }
};

//Arrow Functions were created to act as a shortcut to writing "function". It looks something like this:(parameters) =>" followed by brackets.
 
