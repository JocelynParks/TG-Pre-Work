
function monitorCount (rows, columns) {

return rows*columns;
}
const numOfMonitors = monitorCount(5, 4);

 console.log(numOfMonitors);

//Automatically, a called function is determined "undefined". With the use of the keyword "return", functions will produce an output
