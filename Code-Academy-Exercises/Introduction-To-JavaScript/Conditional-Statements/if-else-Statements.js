let sale = true;

sale = false;

if(sale) {
  console.log('Time to buy!');
} else {
  console.log('Time to wait for a sale.')
}

//With if else statements, you are able to deem conditions false and the code will still run. "Else" adds a default behavior to an "if" statement. They must be paired together followed by {the statement}. This function is useful for binary decisions (yes-or-no questions).
