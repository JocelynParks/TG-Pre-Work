let hungerLevel = 7;
if (hungerLevel > 7) {
 console.log('Time to eat!')
  } else {
 console.log('We can eat later!')
               }
//Comparison values are used to compare values. The following may be used: <, >, <=, >=, === (equal to), !== (not equal to). Values must be able to be compared and separated with an operator.
