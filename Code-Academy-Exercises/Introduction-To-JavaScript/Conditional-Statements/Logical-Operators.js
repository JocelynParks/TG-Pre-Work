let mood = 'sleepy';

let tirednessLevel = 6;
if (mood === 'sleepy' && tirednessLevel > 8){
console.log ('time to sleep')
} else {
console.log ('not bed time yet')
}

// Creating more sophisticated logic by using Boolean values (true or false) and logical operators. There are three logical operators: and (&&)- must be a true statement, or (||)- only one condition must be true, not (!)- reverses the value from true to false or vice versa.
